// It's difficult to implement the Graph in Rust
// And leetcode doesn't support solution using Rust, so I have to skip it
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
